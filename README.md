# Compiler-Design-Lab
Codes for Compiler Design lab (LEX and YACC)
commands to run the code:-
flex filename.l (or lex filename.l)
gcc lex.yy.c
./a.out
use CTRL + d if code not responding or to stop the execution
